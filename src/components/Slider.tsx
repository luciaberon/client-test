import React, { ReactElement } from 'react'

interface Props {
    
}

function Slider({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default Slider
